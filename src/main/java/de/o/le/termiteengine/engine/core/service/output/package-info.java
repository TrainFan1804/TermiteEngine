/**
 * The {@link OutputService} has it own package because this is a complex system
 * that relies on multiple classes.
 * <p>
 * It could be that in a future version more services will have there own package
 * because they grow to fast.
 */
package de.o.le.termiteengine.engine.core.service.output;
